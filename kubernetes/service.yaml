apiVersion: v1
kind: Service
metadata:
  name: sae-app-service
  namespace: sae-production
  labels:
    app: sae-app
spec:
  type: NodePort
  selector:
    app: sae-app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
